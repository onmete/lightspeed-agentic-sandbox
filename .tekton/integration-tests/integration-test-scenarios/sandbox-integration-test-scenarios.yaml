# IntegrationTestScenario resources for Konflux (appstudio.redhat.com/v1beta2).
# Apply to crt-nshift-lightspeed-tenant. Each scenario triggers the shared
# sandbox-integration-test-pipeline with provider-specific credentials.
#
# Naming: {model-or-vendor}-{transport?}-{runtime}
# These replace the Prow e2e-* presubmit jobs.
---
apiVersion: appstudio.redhat.com/v1beta2
kind: IntegrationTestScenario
metadata:
  name: sandbox-integration-anthropic-vertex-deepagents
  namespace: crt-nshift-lightspeed-tenant
  labels:
    test.appstudio.openshift.io/optional: "true"
spec:
  application: ols
  contexts:
    - description: Sandbox integration test — Anthropic via Vertex + DeepAgents
      name: component_lightspeed-agentic-sandbox
  params:
    - name: provider
      value: anthropic-vertex-deepagents
    - name: credential-secret
      value: vertex-apitoken
  resolverRef:
    resolver: git
    resourceKind: pipeline
    params:
      - name: url
        value: https://github.com/openshift/lightspeed-agentic-sandbox
      - name: revision
        value: main
      - name: pathInRepo
        value: .tekton/integration-tests/pipelines/sandbox-integration-test-pipeline.yaml
---
apiVersion: appstudio.redhat.com/v1beta2
kind: IntegrationTestScenario
metadata:
  name: sandbox-integration-gemini-vertex-adk
  namespace: crt-nshift-lightspeed-tenant
  labels:
    test.appstudio.openshift.io/optional: "true"
spec:
  application: ols
  contexts:
    - description: Sandbox integration test — Gemini via Vertex + ADK
      name: component_lightspeed-agentic-sandbox
  params:
    - name: provider
      value: gemini-vertex-adk
    - name: credential-secret
      value: vertex-apitoken
  resolverRef:
    resolver: git
    resourceKind: pipeline
    params:
      - name: url
        value: https://github.com/openshift/lightspeed-agentic-sandbox
      - name: revision
        value: main
      - name: pathInRepo
        value: .tekton/integration-tests/pipelines/sandbox-integration-test-pipeline.yaml
---
apiVersion: appstudio.redhat.com/v1beta2
kind: IntegrationTestScenario
metadata:
  name: sandbox-integration-openai-agents
  namespace: crt-nshift-lightspeed-tenant
  labels:
    test.appstudio.openshift.io/optional: "true"
spec:
  application: ols
  contexts:
    - description: Sandbox integration test — OpenAI Agents SDK
      name: component_lightspeed-agentic-sandbox
  params:
    - name: provider
      value: openai-agents
    - name: credential-secret
      value: openai
  resolverRef:
    resolver: git
    resourceKind: pipeline
    params:
      - name: url
        value: https://github.com/openshift/lightspeed-agentic-sandbox
      - name: revision
        value: main
      - name: pathInRepo
        value: .tekton/integration-tests/pipelines/sandbox-integration-test-pipeline.yaml
